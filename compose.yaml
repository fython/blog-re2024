services:
  app:
    image: node:24
    ports:
      - 4321:4321
    working_dir: /app
    command: >
      bash -c "
      corepack enable && corepack prepare pnpm@10.6.4 --activate
      && pnpm install --frozen-lockfile
      && pnpm prune
      && pnpm run dev --host 0.0.0.0"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4321"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 60s
    volumes:
      - ./:/app
      - /app/node_modules
